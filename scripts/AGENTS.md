# Scripts Overview

This folder contains start and stop scripts for Windows, macOS, and Linux.

## Current Scripts

- start.ps1 / stop.ps1: Windows PowerShell scripts to build and run the container.
- start.sh / stop.sh: macOS/Linux scripts to build and run the container.